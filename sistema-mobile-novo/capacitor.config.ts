import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.emporiodocerrado.mobile',
  appName: 'emporio-mobile',
  webDir: 'www'
};

export default config;
